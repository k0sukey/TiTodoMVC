(function(tirequire,__dirname,__filename){module.id=__filename;module.loaded=false;module.filename=__filename;var require=tirequire("node_modules/ti-commonjs/lib/ti-commonjs")(__dirname,module);module.require=require;module.exports = function(grunt) {
    grunt.registerTask("check-sauce-creds", function() {
        if (void 0 === process.env.SAUCE_USERNAME) {
            grunt.log.writeln("No sauce credentials found");
            grunt.task.clearQueue();
        } else grunt.log.writeln("Sauce credentials found");
    });
};
module.loaded=true;})(require,"/node_modules/moment/tasks","/node_modules/moment/tasks/check_sauce_creds.js");